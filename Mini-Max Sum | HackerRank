import math
import os
import random
import re
import sys

def miniMaxSum(arr):
    sum_val = sum(arr)
    print( sum_val-max(arr), sum_val-min(arr))

if __name__ == '__main__':
    arr = list(map(int, input().rstrip().split()))

    miniMaxSum(arr)
